import React from 'react';
import { render, cleanup } from 'react-native-testing-library';

import Button from '../../../components/buttons/OpacityButton';

describe("TouchableOpacity Button", () => {
  test("test", () => {
    expect(true).toBeTruthy()
  })
})
